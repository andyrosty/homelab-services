apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: homepage
  namespace: homepage
spec:
  interval: 10m
  chart:
    spec:
      chart: homepage
      sourceRef:
        kind: HelmRepository
        name: homepage
        namespace: flux-system
      interval: 1h
  values:
    image:
      repository: ghcr.io/gethomepage/homepage
      tag: latest

    service:
      main:
        type: ClusterIP
        ports:
          http:
            port: 3000

    ingress:
      main:
        enabled: true
        className: nginx
        hosts:
          - host: homepage.dev-andrew.com
            paths:
              - path: /
                pathType: Prefix

    config:
      settings:
        title: Andrew's Homelab
        theme: dark
        color: slate
        headerStyle: boxed

      services:
        - Infrastructure:
            - Proxmox:
                href: https://10.0.0.214:8006
                description: Virtualization host
            - OPNsense:
                href: https://192.168.50.1
                description: Homelab firewall/router
            - Smoke Test:
                href: http://smoke-test.dev-andrew.com
                description: Kubernetes ingress test app

      bookmarks:
        - DevOps:
            - GitHub:
                - abbr: GH
                  href: https://github.com/andyrosty
            - Kubernetes Docs:
                - abbr: K8s
                  href: https://kubernetes.io/docs/
