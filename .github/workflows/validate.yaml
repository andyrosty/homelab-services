name: Validate GitOps Manifests

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  schedule:
    - cron: "17 2 * * *"
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

env:
  KUSTOMIZE_VERSION: ${{ vars.KUSTOMIZE_VERSION }}
  KUBECONFORM_VERSION: ${{ vars.KUBECONFORM_VERSION }}
  TRIVY_VERSION: ${{ vars.TRIVY_VERSION }}
  CLUSTER_KUSTOMIZE_PATH: "clusters/homelab"
  TRIVY_EXIT_CODE: "0"

jobs:
  yaml-lint:
    name: YAML lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install yamllint
        run: |
          sudo apt-get update
          sudo apt-get install -y yamllint

      - name: Lint YAML
        run: |
          yamllint .

  kustomize-build:
    name: Kustomize build
    runs-on: ubuntu-latest
    needs:
      - yaml-lint

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install kustomize
        run: |
          KUSTOMIZE_URL="https://github.com/kubernetes-sigs/kustomize/releases/download/"
          KUSTOMIZE_URL+="kustomize/v${KUSTOMIZE_VERSION}/"
          KUSTOMIZE_URL+="kustomize_v${KUSTOMIZE_VERSION}_linux_amd64.tar.gz"

          echo "Downloading: ${KUSTOMIZE_URL}"

          curl -fL "${KUSTOMIZE_URL}" -o kustomize.tar.gz
          tar -xzf kustomize.tar.gz
          chmod +x kustomize
          sudo mv kustomize /usr/local/bin/kustomize
          kustomize version

      - name: Build homelab manifests
        run: |
          mkdir -p /tmp/rendered
          kustomize build clusters/homelab > /tmp/rendered/homelab.yaml
          wc -l /tmp/rendered/homelab.yaml

      - name: Upload rendered manifests
        uses: actions/upload-artifact@v4
        with:
          name: rendered-homelab-manifests
          path: /tmp/rendered/homelab.yaml

  kubeconform:
    name: Kubernetes schema validation
    runs-on: ubuntu-latest
    needs:
      - kustomize-build

    steps:
      - name: Download rendered manifests
        uses: actions/download-artifact@v4
        with:
          name: rendered-homelab-manifests
          path: /tmp/rendered

      - name: Install kubeconform
        run: |
          KUBECONFORM_VERSION="0.6.7"
          curl -fL "https://github.com/yannh/kubeconform/releases/download/v${KUBECONFORM_VERSION}/kubeconform-linux-amd64.tar.gz" -o kubeconform.tar.gz
          tar -xzf kubeconform.tar.gz
          sudo mv kubeconform /usr/local/bin/kubeconform
          kubeconform -v

      - name: Validate Kubernetes schemas
        run: |
          kubeconform \
            -strict \
            -summary \
            -ignore-missing-schemas \
            /tmp/rendered/homelab.yaml

  trivy-config:
    name: Trivy config security scan
    runs-on: ubuntu-latest
    needs:
      - kustomize-build

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Trivy
        run: |
          TRIVY_VERSION="0.69.3"
          curl -fL "https://github.com/aquasecurity/trivy/releases/download/v${TRIVY_VERSION}/trivy_${TRIVY_VERSION}_Linux-64bit.tar.gz" -o trivy.tar.gz
          file trivy.tar.gz
          tar -xzf trivy.tar.gz
          sudo mv trivy /usr/local/bin/trivy
          trivy --version

      - name: Scan Kubernetes manifests
        run: |
          trivy config \
            --severity HIGH,CRITICAL \
            --exit-code 0 \
            --skip-version-check \
            --skip-dirs infrastructure/metallb/manifests \
            --skip-dirs clusters/homelab/flux-system \
            .
            
  upload-report:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate sample report
        run: |
          mkdir -p reports
          {
            echo "Repository: $GITHUB_REPOSITORY"
            echo "Branch: $GITHUB_REF_NAME"
            echo "Commit: $GITHUB_SHA"
            echo "Run ID: $GITHUB_RUN_ID"
            echo "Generated at: $(date -u)"
          } > reports/build-report.txt

      - name: Upload report to Nexus
        env:
          NEXUS_URL: ${{ secrets.NEXUS_URL }}
          NEXUS_USERNAME: ${{ secrets.NEXUS_USERNAME }}
          NEXUS_PASSWORD: ${{ secrets.NEXUS_PASSWORD }}
          CF_ACCESS_CLIENT_ID: ${{ secrets.CF_ACCESS_CLIENT_ID }}
          CF_ACCESS_CLIENT_SECRET: ${{ secrets.CF_ACCESS_CLIENT_SECRET }}
        run: |
          REPORT_PATH="${GITHUB_REPOSITORY}/${GITHUB_REF_NAME}/${GITHUB_RUN_ID}/build-report.txt"

          curl --fail \
            -H "CF-Access-Client-Id: $CF_ACCESS_CLIENT_ID" \
            -H "CF-Access-Client-Secret: $CF_ACCESS_CLIENT_SECRET" \
            --user "$NEXUS_USERNAME:$NEXUS_PASSWORD" \
            --upload-file reports/build-report.txt \
            "$NEXUS_URL/repository/raw-reports/$REPORT_PATH"

